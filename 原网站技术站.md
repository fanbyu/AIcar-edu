技术栈确认与自建可行性分析
1. 网页技术栈确认：确为 TensorFlow.js 构建
根据前文分享实录，汤老师明确提到其学具配套网站“借助于这个 TensorFlowJS（浏览器端的人工智能开源框架）”来实现神经网络训练与推理。这与 TensorFlow.js 的官方定位完全吻合——它是一款开源的 JavaScript 库，允许开发者完全在浏览器中使用 JS 定义、训练和运行机器学习模型，用户打开网页即可运行，无需安装任何本地驱动或库。
2. 自己搭建此类“服务器”完全可行，且无需重型后端
• 算力在客户端，服务端极轻量：TensorFlow.js 的核心设计理念是客户端计算。模型的训练与推理均在访客浏览器内执行，数据始终保留在客户端设备（隐私友好），这省去了远程服务器的算力开销与复杂运维。因此你不需要采购配备 GPU 的推理/训练服务器，只需提供静态资源访问能力。
• 具体搭建方式：官方入门示例显示，TF.js 可直接通过 <script> 标签从 CDN 加载，开发者仅需编写 HTML/CSS/JS 文件并托管在简易 Web 环境（如 GitHub Pages、普通静态虚拟主机，或本地测试用 Python SimpleServer 规避 CORS）即可运行。汤老师的实践也是建立一个免费、免注册的公网网页，学生点击链接就能使用。
• 内网/离线适配：针对学校无外网或防 distraction 场景，可如交流中提到的将其封装为 PWA（渐进式 Web 应用），一次加载后彻底离线可用，且 CDN 托管成本极低甚至为零。
3. 主要算力确实由学生端浏览器完成
• 端侧推理与训练：无论是最简单的 MobileNet+KNN、适中的 MLP，还是高阶自定义 CNN，其图像特征提取、模型拟合及实时分类概率输出，都运行在学生自己的手机、平板或笔记本电脑的浏览器内核中，并自动调用 WebGL 进行 GPU 加速。
• 软硬件清晰分工：按照汤老师方案，ESP32 等开源硬件仅承担两种角色——若用 ESP32-CAM 则采集画面传回浏览器，或接收浏览器下发的蓝牙指令驱动电机；真正的“智能脑力”全在浏览器端。这也是整套学具能实现百元级成本、规避学校专项经费审批的根本原因。